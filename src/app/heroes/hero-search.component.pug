#search-component
  h4 Hero search
  input(#searchBox (keyup)="search(searchBox.value)")
  ul
    li.search-result(
      *ngFor="let hero of heroes | async",
      (click)="gotoDetail(hero)"
    ) {{hero.name}}
