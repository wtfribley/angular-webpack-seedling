h2 My Heroes
ul
  li(
    [class.selected]="hero === selectedHero",
    *ngFor="let hero of heroes",
    (click)="onSelect(hero)"
  )
    span {{hero.id}} | {{hero.name}}
    button((click)="deleteHero(hero, $event)") Delete

div.error(*ngIf="error") {{error}}

button((click)="addHero()") Add new hero
div(*ngIf="addingHero")
  my-hero-detail((close)="close($event)")

div(*ngIf="selectedHero")
  h2 {{selectedHero.name | uppercase}} is my hero!
  button((click)="gotoDetail()") View details
